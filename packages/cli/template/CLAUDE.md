# CLAUDE.md

All open-canvas authoring rules live in [`AGENTS.md`](./AGENTS.md). Claude Code reads this file by convention; its entire normative content is delegated to `AGENTS.md` so every agent (Claude, Codex, Cursor, Copilot, …) follows the exact same contract.
